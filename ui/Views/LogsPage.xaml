<UserControl x:Class="SSnetUI.Views.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Логи" Style="{StaticResource H1}"/>
            <TextBlock Text="Live-вывод процесса sing-box (доступен только когда UI сам запустил процесс)"
                       Style="{StaticResource Dim}" Margin="0,4,0,16" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Очистить" Style="{StaticResource SecondaryButton}"
                    Click="Clear_Click" Margin="0,0,8,0"/>
            <CheckBox x:Name="AutoScroll" Content="Автопрокрутка" IsChecked="True"
                      VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <TextBox x:Name="Log" IsReadOnly="True"
                     Background="{StaticResource BgInputBrush}"
                     Foreground="{StaticResource TextBrush}"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="NoWrap"
                     FontFamily="Consolas, Cascadia Mono, Courier New"
                     FontSize="12"
                     BorderThickness="0" Padding="12"/>
        </Border>
    </Grid>
</UserControl>
